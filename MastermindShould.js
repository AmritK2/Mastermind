var expect = require('chai').expect; // gives access to files
var checkAnswer = require("./Mastermind");

describe("RandomlySelectedColourShould", function() {
    it("return true", function() {

        var guessedColours = "Red, Orange, Yellow, Yellow";

        // var guessedColours = new RegExp("Red, Orange, Yellow, Yellow");
        //var result = guessedColours.test(mastermindSelectedColours);
        expect(true).to.equal(output);
    });
});